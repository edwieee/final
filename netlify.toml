[build]
  command = "npm run build"
  publish = ".next"

# Ensure Next.js is handled correctly on Netlify
[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  node_bundler = "esbuild"










